<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.requestapp">

    <application
        android:allowBackup="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyNewRequest_page_"
            android:label="@string/title_activity_my_new_request_page_"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Inbox_page"

            android:label="@string/title_activity_inbox_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ForgotPassword"
             />
        <activity
            android:name=".MyRequest"

            android:label="@string/title_activity_my_request"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
           >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>